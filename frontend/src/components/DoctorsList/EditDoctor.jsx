import { useEffect, useState } from 'react';
import {
  Input,
  InputLabel,
  Button
} from '@mui/material';

const EditDoctorForm = (props) => {
  const {id} = props;

  
  
  return(
    <form>

    </form>
  )
}

export default EditDoctorForm;